<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/CustomActionbar</item>
    </style>

    <style name="CustomActionbar" parent="android:Widget.ActionBar">
        <item name="android:background">@color/blue_dark</item>
        <item name="android:titleTextStyle">@style/CustomActionbar.TextStyle</item>
    </style>

    <style name="CustomActionbar.TextStyle" parent="android:TextAppearance.DeviceDefault.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>
